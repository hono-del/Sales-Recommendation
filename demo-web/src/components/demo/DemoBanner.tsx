export function DemoBanner() {
  return (
    <div
      className="border-b border-gold/30 bg-gold/10 px-4 py-2 text-center text-sm text-navy"
      role="status"
    >
      デモデータで表示中（オフライン / Neo4j 未接続時）
    </div>
  );
}
