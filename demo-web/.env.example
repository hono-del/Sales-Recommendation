# API URL (default: localhost for development)
# Windows では localhost が IPv6 になり API に届かないことがあるため 127.0.0.1 推奨
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# For production deployment (Vercel), set this to your deployed API URL
# NEXT_PUBLIC_API_URL=https://your-api-url.com
